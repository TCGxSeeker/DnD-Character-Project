cache=.npm-cache
fund=false
audit=false
# npm 11 blocks dependency install scripts unless the project explicitly
# trusts them. These scripts install the platform binaries used by Vite and
# the Windows release builder; they never run in the packaged application.
allow-scripts[]=esbuild
allow-scripts[]=electron
allow-scripts[]=electron-winstaller
